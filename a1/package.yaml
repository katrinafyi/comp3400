name: a1
version: 1.0

dependencies:
  - base
  - QuickCheck

library:
  exposed-modules:
    - ApplyAll
    - CartProd
    - Checksum
    - Magic
    # - Boolean
  source-dirs: src
  ghc-options: []
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

